import 'package:flutter/foundation.dart';

/// Global preference for the elderly keyboard feature.
/// Defaults to `false` (disabled by default).
final ValueNotifier<bool> elderlyKeyboardEnabled = ValueNotifier<bool>(false);
